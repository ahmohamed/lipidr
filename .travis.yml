# Sample .travis.yml for R projects
language: r
warnings_are_errors: false

matrix:
  include:
    - os: linux
      dist: trusty
      r: bioc-devel
    - os: osx
      r: bioc-release

env:
 global:
   - CRAN: http://cran.rstudio.com

notifications:
  email:
    on_success: change
    on_failure: change
